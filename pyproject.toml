[project]
name = "cecam-no-harness"
version = "0.1.0"
description = "The bare catalysis fitting task — no harness."
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
    "matplotlib>=3.8",
    "pandas>=2.0",
]
